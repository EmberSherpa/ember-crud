import { arrayContains } from '../../../helpers/array-contains';
import { module, test } from 'qunit';

module('Unit | Helper | array contains');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = arrayContains(42);
  assert.ok(result);
});
