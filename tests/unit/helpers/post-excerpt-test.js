import { postExcerpt } from '../../../helpers/post-excerpt';
import { module, test } from 'qunit';

module('Unit | Helper | post excerpt');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = postExcerpt(42);
  assert.ok(result);
});
